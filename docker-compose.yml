services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tradingpro-app
    environment:
      - NODE_ENV=production
      - PORT=4500
      - MONGODB_URI=${MONGODB_URI}
    ports:
      - "4500:4500"
    restart: unless-stopped
